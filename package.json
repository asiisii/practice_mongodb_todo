{
	"name": "react-ts-tailwindrtk-query-mongodb-",
	"version": "1.0.0",
	"scripts": {
		"client": "cd src/client && yarn start",
		"server": "cd src/server && yarn start",
		"dev": "concurrently --kill-others-on-fail \"yarn client\" \"yarn server\""
	},
	"dependencies": {
		"concurrently": "^7.6.0"
	}
}
